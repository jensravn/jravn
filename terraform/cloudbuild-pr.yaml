steps:
  # TF VALIDATE
  - id: "tf validate"
    name: "hashicorp/terraform:1.0.0"
    entrypoint: "sh"
    args:
      - "-c"
      - |
        terraform init
    dir: "terraform"

  # TF INIT
  - id: "tf init"
    name: "hashicorp/terraform:1.0.0"
    entrypoint: "sh"
    args:
      - "-c"
      - |
        terraform init
    dir: "terraform"

  # TF PLAN.
  - id: "tf plan"
    name: "hashicorp/terraform:1.0.0"
    entrypoint: "sh"
    args:
      - "-c"
      - |
        terraform plan
    dir: "terraform"
